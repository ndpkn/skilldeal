mixin formRu
  form#formModal(name="contactFormRu", netlify)
    .modal-content__form-item
      label(for="name") Имя
      input(type="text", id="name", name="name", required, maxlength="20")

    .modal-content__form-item
      label(for="phone") Номер телефона
      input(type="tel", id="phone", name="phone", required)
      span#error-msg.hide

    button#submit.ctaButton(type="submit") Заказать консультацию